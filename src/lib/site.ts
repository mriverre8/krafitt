/** Where the project lives, for the links in the footer. */

/** The app's own name, wherever it is shown as itself rather than translated:
    the tab title, the iOS home-screen label, the wordmark's alt text. */
export const APP_NAME = 'Krafitt';

const GITHUB_USER = 'mriverre8';

export const REPO_URL = `https://github.com/${GITHUB_USER}/krafitt`;
export const PROFILE_URL = `https://github.com/${GITHUB_USER}`;
export const CONTACT_EMAIL = 'krafitt@gmail.com';
