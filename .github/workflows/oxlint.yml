name: oxlint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  oxlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # oxlint is a standalone binary: no install, no config, no deps.
      - run: npx --yes oxlint@1.82.0 src
